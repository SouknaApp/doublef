{
  "name": "doublef",
  "version": "1.0.0",
  "description": "Double F — Truth & Marry game",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --watch",
    "test": "echo \"No tests configured\" && exit 0",
    "build": "echo \"static site — no build step\""
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.3.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
